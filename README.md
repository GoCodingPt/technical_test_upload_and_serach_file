If you still want run backend: pnpm --filter "backend" dev
If you still want run backend: pnpm --filter "frontend" dev

If you want run everthing package: pnpm --filter "\*\*" dev

OBS: Is necessary to remove "\*\*" bars up.
